%div{'data-role' => 'content'}
  %ul{'data-role' => 'listview'}
    - @favourites.each do |favourite|
      %li
        = link_to favourite.name,
          search_schedule_search_path(:station_from => favourite.station_from,
                                      :station_to => favourite.station_to,
                                      :from_station => 'true') 
        = link_to 'Edytuj', edit_favourite_path(favourite), 'data-icon' => 'gear'
  %br
  = link_to 'Dodaj', new_favourite_path, 'data-icon' => 'plus', 'data-role' => 'button'
