<!DOCTYPE html>
%html
  %head
    %title rozkład.czest.pl - Jedyny serwis wyszukujący połączenia komunikacji miejskiej w Częstochowie.
    %meta{"http-equiv" => "Content-Type", "content" => "text/html; charset=UTF-8"}
    = stylesheet_link_tag 'jquery.ui', 'default'
    = javascript_include_tag :defaults, 'jquery.ui', 'autocomplete-rails', 'http://maps.google.com/maps/api/js?sensor=true'
    - if session[:mobile]
      %meta{:content => "width=device-width, initial-scale=1", :name => "viewport"}
      %link{:href => "images/iphone_icon.png", :rel => "apple-touch-icon"}
      = stylesheet_link_tag 'mobile', 'jquery.mobile.css'
      = javascript_include_tag 'jquery.mobile.js'
    - else
      = stylesheet_link_tag '960', 'reset', 'text', 'desktop'
    = csrf_meta_tag
  %body{:onload => current_action?('map') ? 'getMap();' : nil}
    .container_12{'data-role' => 'page', :style => (current_action?('map') && session[:mobile]) ? 'width:100%; height:100%;' : nil}
      = render :partial => 'layouts/header'
      = render :partial => 'layouts/content'
    - unless session[:mobile]
      .clear
      = render :partial => 'layouts/footer'
